#include <stdio.h>
#include <time.h>

int main()
{
	//time_t time(time_t local)
	//struct tm *local_time(const time_t *time)
	//struct tm *gmtime(const time_t *time)

	//char *asctime(const struct tm *ptr)
	//char *ctime(const time_t *time)


	return 0;
}